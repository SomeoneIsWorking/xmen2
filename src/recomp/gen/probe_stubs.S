/* GENERATED by tools/gen_probes.py from tools/probes.json -- do not edit.
 *
 * The STOCK side's hook bodies, one per probe, for 32-bit mingw.
 *
 * The game's own libIGMath is patched at the entry of each probed function
 * with a jump to the matching stub here. A stub records the arguments, calls
 * the original through a trampoline (the relocated prologue plus a jump back),
 * records what the call wrote, and returns exactly as the original would.
 *
 * WHY GENERATED, and not one hand-written stub with a runtime argument count:
 * re-pushing the arguments and cleaning the frame both depend on the size of
 * that frame, and doing it from a table means computing stack offsets in asm
 * at run time inside the game's own control flow. Baking each count into its
 * own stub makes every offset here a constant the assembler checks.
 *
 * The x87 stack is saved and restored around the recorder because these are
 * FLOATING-POINT functions and the C recorder calls into the game's CRT to
 * write the file. Clobbering ST0..7 mid-animation would corrupt the very
 * values the harness exists to measure -- it would be an instrument that
 * changes the answer.
 */
    .text

/* Gap::Math::igQuaternionf::slerp -- 12 argument byte(s), callee-cleaned */
    .globl _probe_stub_0
_probe_stub_0:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $0
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 16(%ebp)
    push 12(%ebp)
    push 8(%ebp)
    call *(_probe_tramp + 0)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $0
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $12

/* Gap::Math::igQuaternionf::lerp -- 12 argument byte(s), callee-cleaned */
    .globl _probe_stub_1
_probe_stub_1:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $1
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 16(%ebp)
    push 12(%ebp)
    push 8(%ebp)
    call *(_probe_tramp + 4)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $1
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $12

/* Gap::Math::igQuaternionf::normalize -- 0 argument byte(s), caller-cleaned */
    .globl _probe_stub_2
_probe_stub_2:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $2
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    call *(_probe_tramp + 8)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $2
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret

/* Gap::Math::igQuaternionf::getMatrix -- 4 argument byte(s), callee-cleaned */
    .globl _probe_stub_3
_probe_stub_3:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $3
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 8(%ebp)
    call *(_probe_tramp + 12)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $3
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $4

/* Gap::Math::igQuaternionf::multiply -- 8 argument byte(s), callee-cleaned */
    .globl _probe_stub_4
_probe_stub_4:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $4
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 12(%ebp)
    push 8(%ebp)
    call *(_probe_tramp + 16)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $4
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $8

/* Gap::Math::igMatrix44f::multiplyAligned -- 8 argument byte(s), callee-cleaned */
    .globl _probe_stub_5
_probe_stub_5:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $5
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 12(%ebp)
    push 8(%ebp)
    call *(_probe_tramp + 20)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $5
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $8

/* Gap::Math::igMatrix44f::setQuaternion -- 4 argument byte(s), callee-cleaned */
    .globl _probe_stub_6
_probe_stub_6:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $6
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 8(%ebp)
    call *(_probe_tramp + 24)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $6
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $4

/* Gap::Sg::igActor::getBlendMatrix -- 4 argument byte(s), callee-cleaned */
    .globl _probe_stub_7
_probe_stub_7:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $7
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 8(%ebp)
    call *(_probe_tramp + 28)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $7
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $4

/* Gap::Sg::igActor::getBoneMatrix -- 4 argument byte(s), callee-cleaned */
    .globl _probe_stub_8
_probe_stub_8:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $8
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 8(%ebp)
    call *(_probe_tramp + 32)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $8
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $4

/* Gap::Sg::igAnimationCombiner::getBlendMatrix -- 4 argument byte(s), callee-cleaned */
    .globl _probe_stub_9
_probe_stub_9:
    push %ebp
    mov %esp, %ebp
    pushal
    sub $108, %esp
    fnsave (%esp)
    lea 8(%ebp), %eax
    push %eax
    push %ecx
    push $9
    call _probe_enter
    add $12, %esp
    frstor (%esp)
    add $108, %esp
    popal
    push 8(%ebp)
    call *(_probe_tramp + 36)
    pushal
    sub $108, %esp
    fnsave (%esp)
    mov 136(%esp), %eax
    push %eax
    push $9
    call _probe_leave
    add $8, %esp
    frstor (%esp)
    add $108, %esp
    popal
    mov %ebp, %esp
    pop %ebp
    ret $4

    .data
    .align 4
    .globl _probe_stub_table
_probe_stub_table:
    .long _probe_stub_0
    .long _probe_stub_1
    .long _probe_stub_2
    .long _probe_stub_3
    .long _probe_stub_4
    .long _probe_stub_5
    .long _probe_stub_6
    .long _probe_stub_7
    .long _probe_stub_8
    .long _probe_stub_9

/* Filled in by probe_hook.c when each trampoline is built. */
    .globl _probe_tramp
_probe_tramp:
    .space 40
