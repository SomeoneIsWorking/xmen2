#ifndef X2_PROMPT_GLYPH_PACK_H
#define X2_PROMPT_GLYPH_PACK_H

/* One gate for every byte that only the derived prompt font can draw. */
int prompt_glyph_pack_enabled(void);

#endif /* X2_PROMPT_GLYPH_PACK_H */
